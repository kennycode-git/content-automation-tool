[phases.setup]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["pip3.11 install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"